// How a Match Fixed record earned its Verified badge.
// PARTNER — the matched Suhana partner confirmed it themselves (peer confirmation).
// ADMIN   — Suhana staff confirmed it, used when the partner is not a Suhana user.
export enum VerificationMethod {
  PARTNER = 'PARTNER',
  ADMIN = 'ADMIN',
}
